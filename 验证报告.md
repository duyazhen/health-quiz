# 本地验证报告 · 2026-09-21

环境：MySQL 8.0.40；库 `health_quiz` / `health_quiz_test` 已 migrate；`.env` / `.env.test` 已配；`pnpm install` / `prisma db seed` 成功；dev `http://localhost:3000`。
sessionId=`ykd3d8x1bacq4d01ovkf7gi7`  Idempotency-Key=`97f64971-8c69-4081-8737-f13918710fdc`

## curl 主流程

### a. POST /api/session → HTTP 201
`{"sessionId":"ykd3d8x1bacq4d01ovkf7gi7","expiresAt":"2026-09-28T03:08:03.002Z"}`

### b. PATCH step=1 gender=male → HTTP 200
`{"data":{"gender":"male","goal":null,"age":null,...},"stepCompleted":1,"expiresAt":"2026-09-28T03:08:03.756Z"}`

### c. 重复 PATCH 同 payload（幂等）→ HTTP 200
数据不变，`stepCompleted` 仍为 1；`expiresAt` 续期到 `03:08:04.371Z`

### d. PATCH step=3 age=25 → HTTP 200
`gender=male` 保留，`age=25`，`stepCompleted=3`

### e. GET /api/progress → HTTP 200 已合并
`{"data":{"gender":"male","goal":null,"age":25,...},"stepCompleted":3}`

### f. POST /api/assessment/submit → HTTP 201
`{"resultId":"cmuao0pte000nukkc88rbbzsx"}`

### g. GET /api/result 未付费 → HTTP 200 脱敏
`{"bmi":22.9,"recommendedCalories":2594}`  （无 targetDate / weightProjection）

### h. POST /api/pay → HTTP 200
`{"code":0,"message":"ok","data":{"subscriptionId":"cmuao0nd2000jukkcrbug9bci","status":"active"},"requestId":"98e4ce5c-c79e-41d9-9d9e-932f8a12f0f0"}`

### i. POST /api/pay 同 Key → HTTP 200 幂等
同一 `subscriptionId`，未新建订阅

### j. GET /api/result 已付费 → HTTP 200 完整 DTO
含 `targetDate":"2026-11-30"`、`algorithmVersion":"v1.0-Mifflin-StJeor"`、`weightProjection`（week 0–10，70kg→65kg）

## 非法值

### PATCH age=abc → HTTP 400
`{"code":400,"message":"Invalid request","details":{"fieldErrors":{"data":["Expected number, received string"]}}}`

### POST /api/pay planType=hack → HTTP 400
`{"code":400,"message":"Invalid request","details":{"fieldErrors":{"planType":["Invalid enum value. Expected 'monthly' | 'yearly', received 'hack'"]}}}`

## 测试

`pnpm test` 连续两轮均为 **15/15 通过**（算法 5 + API 集成 10），命令与 CI 一致。
